<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User History</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body tabindex="-1">

  <!-- Navbar -->
  <!-- Include your navbar.ejs content here -->

  <div class="container mx-auto p-4">

    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <% if (session && session.user) { %>
            <h1 class="text-5xl font-bold">User History for <%= session.user.userName %></h1>
            <p class="text-xl"><%= session.user.email %></p>
          <% } else { %>
            <h1 class="text-5xl font-bold">User History</h1>
            <p class="py-6">Login to view your history.</p>
          <% } %>
        </div>
      </div>
    </div>

    <br />

    <% if (session && session.user && session.user.loginHistory && session.user.loginHistory.length > 0) { %>
      <table class="table w-full">
        <thead>
          <tr>
            <th>Login Date/Time</th>
            <th>Client Information</th>
          </tr>
        </thead>
        <tbody>
          <% session.user.loginHistory.forEach(historyItem => { %>
            <tr>
              <td><%= historyItem.dateTime %></td>
              <td><%= historyItem.userAgent %></td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    <% } else { %>
      <% if (session && session.user) { %>
        <p>No login history available for <%= session.user.userName %>.</p>
      <% } else { %>
        <p>Login to view your history.</p>
      <% } %>
    <% } %>

  </div>

</body>

</html>
